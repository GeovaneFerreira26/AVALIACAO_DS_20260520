version: '3.8'

services:
  db:
    image: mysql:latest
    container_name: mysql_projeto
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: db_avalicao20260521
    ports:
      - "3307:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data: